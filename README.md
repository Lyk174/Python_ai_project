# 请配合前端文件：https://github.com/Lyk174/frontend
# 进虚拟环境：venv312\Scripts\Activate.ps1
# 下载依赖文件：pip install -r requirements.txt
# chroma run --path ./chroma_db --host 0.0.0.0 --port 8001
#  python -c "import nltk; print(nltk.data.find('tokenizers/punkt_tab.zip'))"
# celery -A celery_worker worker --loglevel=info --pool=solo
# $env:HF_ENDPOINT = "https://hf-mirror.com"
# $env:NLTK_DATA = "C:\Users\admin\nltk_data"
# 运行命令：uvicorn app.main:app --reload
# 生成新的依赖文件pip freeze > requirements.txt
